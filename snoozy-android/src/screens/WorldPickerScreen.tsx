import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

export default function WorldPickerScreen() {
  return (
    <View style={styles.container}>
      <Text>WorldPickerScreen</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  container: { flex: 1 },
})
